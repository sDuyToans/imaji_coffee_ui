import React, { Component } from 'react';
import ReactDom from 'react-dom';
import ReactJustified from '../../lib/reactJustified';

// EXAMPLE 1
const defaultOptions = {
	boxes: [0.5, 1.5, 1, 1.8, 0.4, 0.7, 0.9, 1.1, 1.7, 2, 2.1],
	containerWidth: 530,
	containerPadding: 5,
	boxSpacing: 5,
	targetRowHeight: 160,
};

ReactDom.render(
	<ReactJustified
		options={ defaultOptions }
	    boxClass={ 'justified-layout-box--custom' }
	    containerClass={ 'justified-layout-container--custom' }
	/>,
	document.getElementById('default')
);

// EXAMPLE 2
const containerPaddingConfig = Object.assign({}, defaultOptions, {
	containerPadding: 25,
	boxes: [0.5, 1.5, 1, 1.8, 0.4, 0.7, 0.9, 1.1, 1.7],
});

ReactDom.render(
	<ReactJustified
		options={ containerPaddingConfig }
	    boxClass={ 'justified-layout-box--custom' }
	    containerClass={ 'justified-layout-container--custom' }
	/>,
	document.getElementById('container-padding')
);

// EXAMPLE 3
const unevenContainerPaddingConfig = Object.assign({}, defaultOptions, {
	containerPadding: {
		top: 25,
		right: 10,
		left: 100,
		bottom: 25,
	},
	boxes: [0.5, 1.5, 1, 1.8, 0.4, 0.7, 0.9, 1.1, 1.7],
});

ReactDom.render(
	<ReactJustified
		options={ unevenContainerPaddingConfig }
	    boxClass={ 'justified-layout-box--custom' }
	    containerClass={ 'justified-layout-container--custom' }
	/>,
	document.getElementById('uneven-container-padding')
);

// EXAMPLE 4
const boxSpacingConfig = Object.assign({}, defaultOptions, {
		boxSpacing: {
		horizontal: 2,
		vertical: 15,
	},
	boxes: [0.5, 1.5, 1, 1.8, 0.4, 0.7, 0.9, 1.1, 1.7],
});

ReactDom.render(
	<ReactJustified
		options={ boxSpacingConfig }
	    boxClass={ 'justified-layout-box--custom' }
	    containerClass={ 'justified-layout-container--custom' }
	/>,
	document.getElementById('box-spacing')
);

// EXAMPLE 5
const targetRowHeightConfig = Object.assign({}, defaultOptions, {
	targetRowHeight: 50,
	boxes: [0.5, 1.5, 1, 1.8, 0.4, 0.7, 0.9, 1.1, 1.7, 2.2, 1.5, 0.5, 1.5, 1, 1.8, 0.4, 0.7, 0.9, 1.1, 1.7, 2.2],
});

ReactDom.render(
	<ReactJustified
		options={ targetRowHeightConfig }
	    boxClass={ 'justified-layout-box--custom' }
	    containerClass={ 'justified-layout-container--custom' }
	/>,
	document.getElementById('target-row-height')
);

// EXAMPLE 6
const forceAspectRatioConfig = Object.assign({}, defaultOptions, {
	forceAspectRatio: 1,
	boxes: [0.5, 1.5, 1, 1.8, 0.4, 0.7, 0.9, 1.1, 1.7, 2.2, 1.5],
});

ReactDom.render(
	<ReactJustified
		options={ forceAspectRatioConfig }
	    boxClass={ 'justified-layout-box--custom' }
	    containerClass={ 'justified-layout-container--custom' }
	/>,
	document.getElementById('force-aspect-ratio')
);

// EXAMPLE 7
const widowConfig = Object.assign({}, defaultOptions, {
	boxes: [0.5, 1.5, 1, 1.8, 0.4, 0.7, 0.9, 1.1, 1.7, 2.2],
	showWidows: false,
});

ReactDom.render(
	<ReactJustified
		options={ widowConfig }
	    boxClass={ 'justified-layout-box--custom' }
	    containerClass={ 'justified-layout-container--custom' }
	/>,
	document.getElementById('widow')
);

// EXAMPLE 8
const breakoutRowConfig = Object.assign({}, defaultOptions, {
	boxes: [0.5, 1.5, 1, 1.8, 0.4, 0.7, 0.9, 1.1, 1.7, 2.2, 1.5],
	fullWidthBreakoutRowCadence: 2,
});

ReactDom.render(
	<ReactJustified
		options={ breakoutRowConfig }
	    boxClass={ 'justified-layout-box--custom' }
	    containerClass={ 'justified-layout-container--custom' }
	/>,
	document.getElementById('breakout-row')
);
